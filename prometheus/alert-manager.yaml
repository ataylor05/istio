apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: alertmanager-tls
  namespace: istio-system
spec:
  secretName: alertmanager-tls
  duration: 2160h
  renewBefore: 360h
  subject:
    organizations:
      - teokyllc
  commonName: alertmanager.teokyllc.internal
  isCA: false
  privateKey:
    algorithm: RSA
    encoding: PKCS1
    size: 2048
  usages:
    - server auth
    - client auth
  dnsNames:
    - alertmanager.teokyllc.internal
  issuerRef:
    name: vault-issuer
    kind: ClusterIssuer
    group: cert-manager.io
---
apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: alertmanager-gateway
  namespace: monitoring
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 443
      name: https-alertmanager
      protocol: HTTPS
    hosts:
    - alertmanager.teokyllc.internal
    tls:
      mode: SIMPLE
      credentialName: alertmanager-tls
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: alertmanager-vs
  namespace: monitoring
spec:
  hosts:
  - alertmanager.teokyllc.internal
  gateways:
  - alertmanager-gateway
  http:
  - route:
    - destination:
        host: alertmanager-main
        port:
          number: 9093
